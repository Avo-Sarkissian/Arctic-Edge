---
phase: 02-activity-detection-run-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ArcticEdge/Location/GPSManager.swift
  - ArcticEdge/Activity/ActivityManager.swift
  - ArcticEdgeTests/Activity/ActivityClassifierTests.swift
  - ArcticEdgeTests/Activity/ActivityManagerTests.swift
  - ArcticEdgeTests/Location/GPSManagerTests.swift
  - ArcticEdgeTests/Helpers/MockGPSManager.swift
  - ArcticEdgeTests/Helpers/MockActivityManager.swift
autonomous: true
requirements:
  - DETC-01

must_haves:
  truths:
    - "GPSManager actor yields GPSReading values from CLLocationUpdate.liveUpdates(.otherNavigation) and exposes makeStream() for consumers"
    - "ActivityManager actor bridges CMMotionActivityManager callbacks into AsyncStream<CMMotionActivity> and guards on isActivityAvailable()"
    - "Test stubs exist for all 11 ActivityClassifierTests test cases — all red, all compilable"
    - "MockGPSManager and MockActivityManager protocols allow ActivityClassifier tests to run without real hardware"
  artifacts:
    - path: "ArcticEdge/Location/GPSManager.swift"
      provides: "GPSManager actor with GPSReading model, makeStream(), start(), stop()"
      exports: ["GPSManager", "GPSManager.GPSReading"]
    - path: "ArcticEdge/Activity/ActivityManager.swift"
      provides: "ActivityManager actor wrapping CMMotionActivityManager, makeStream(), start(), stop()"
      exports: ["ActivityManager"]
    - path: "ArcticEdgeTests/Helpers/MockGPSManager.swift"
      provides: "MockGPSManager for injection into ActivityClassifier tests"
    - path: "ArcticEdgeTests/Helpers/MockActivityManager.swift"
      provides: "MockActivityManager for injection into ActivityClassifier tests"
    - path: "ArcticEdgeTests/Activity/ActivityClassifierTests.swift"
      provides: "11 stub @Test functions, all failing with #expect(Bool(false)) placeholders"
    - path: "ArcticEdgeTests/Location/GPSManagerTests.swift"
      provides: "GPSReading model tests (speed validation, accuracy gating)"
    - path: "ArcticEdgeTests/Activity/ActivityManagerTests.swift"
      provides: "ActivityManager mock bridge tests"
  key_links:
    - from: "ArcticEdge/Location/GPSManager.swift"
      to: "CLLocationUpdate.liveUpdates(.otherNavigation)"
      via: "Task { for try await update in CLLocationUpdate.liveUpdates(.otherNavigation) }"
      pattern: "CLLocationUpdate.liveUpdates"
    - from: "ArcticEdge/Activity/ActivityManager.swift"
      to: "CMMotionActivityManager.startActivityUpdates"
      via: "Task { await self?.broadcast(activity) } bridge"
      pattern: "startActivityUpdates"
---

<objective>
Build the two signal-source actors (GPSManager and ActivityManager) that feed the ActivityClassifier, and write all Wave 0 test stubs so subsequent plans execute TDD-correctly.

Purpose: ActivityClassifier cannot exist without injectable signal protocols. The mock protocols are the contracts that make Phase 2 tests simulator-runnable despite CMMotionActivityManager being unavailable in the simulator.

Output: GPSManager actor, ActivityManager actor, injectable mock protocols, 11 test stubs (all red), GPSReading model tests.
</objective>

<execution_context>
@/Users/avosarkissian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/avosarkissian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-activity-detection-run-management/02-CONTEXT.md
@.planning/phases/02-activity-detection-run-management/02-RESEARCH.md
@.planning/phases/02-activity-detection-run-management/02-VALIDATION.md

<interfaces>
<!-- Key types from Phase 1 that this plan uses. No codebase exploration needed. -->

From ArcticEdge/Motion/FilteredFrame.swift:
```swift
nonisolated struct FilteredFrame: Sendable {
    let timestamp: TimeInterval
    let runID: UUID
    let pitch: Double; let roll: Double; let yaw: Double
    let userAccelX: Double; let userAccelY: Double; let userAccelZ: Double
    let gravityX: Double; let gravityY: Double; let gravityZ: Double
    let rotationRateX: Double; let rotationRateY: Double; let rotationRateZ: Double
    let filteredAccelZ: Double
}
```

From ArcticEdge/Motion/StreamBroadcaster.swift:
```swift
actor StreamBroadcaster {
    func makeStream() -> AsyncStream<FilteredFrame>
    func start(runID: UUID) async
    func stop() async
    func broadcast(_ frame: FilteredFrame)
}
```

From ArcticEdge/Session/PersistenceService.swift:
```swift
@ModelActor actor PersistenceService {
    func createRunRecord(runID: UUID, startTimestamp: Date) throws
    func finalizeRunRecord(runID: UUID, endTimestamp: Date) throws
}
```

Established project patterns:
- actor pattern: all non-UI types are actors (MotionManager, StreamBroadcaster, PersistenceService precedent)
- SWIFT_STRICT_CONCURRENCY = complete: nonisolated annotations required on non-isolated members
- Swift Testing: import Testing, @Test, #expect — NO XCTest for new logic
- Test target: ArcticEdgeTests, @testable import ArcticEdge
- Xcode test command: xcodebuild test -scheme ArcticEdge -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -parallel-testing-enabled NO
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GPSManager actor and injectable GPS protocol</name>
  <files>ArcticEdge/Location/GPSManager.swift, ArcticEdgeTests/Location/GPSManagerTests.swift, ArcticEdgeTests/Helpers/MockGPSManager.swift</files>
  <behavior>
    - GPSManager.GPSReading(speed: -1.0, horizontalAccuracy: -1.0, timestamp:) is "invalid GPS" — speed < 0 or accuracy < 0 means unavailable
    - GPSManager.GPSReading(speed: 5.0, horizontalAccuracy: 8.0, timestamp:) is valid skiing speed
    - GPSManager.GPSReading(speed: 2.5, horizontalAccuracy: 5.0, timestamp:) is in chairlift speed range
    - MockGPSManager.inject(reading:) pushes a reading to all active streams
    - MockGPSManager.stop() finishes all streams
  </behavior>
  <action>
    Create ArcticEdge/Location/ directory. Create GPSManager.swift with:

    1. Nested `GPSReading: Sendable` struct: `speed: Double` (m/s, -1 if invalid), `horizontalAccuracy: Double` (meters, <0 if invalid), `timestamp: Date`.

    2. `actor GPSManager` with private stored properties:
       - `backgroundSession: CLBackgroundActivitySession?` — MUST be stored property, not local (deallocation kills GPS)
       - `streamTask: Task<Void, Never>?`
       - `continuations: [UUID: AsyncStream<GPSReading>.Continuation]`

    3. `func start()`: Sets `backgroundSession = CLBackgroundActivitySession()`, then launches `streamTask = Task { [weak self] in for try await update in CLLocationUpdate.liveUpdates(.otherNavigation) { ... } }`. Inside loop: guard `update.location != nil`, create GPSReading preserving raw values (do NOT filter speed == -1 here — filtering is classifier's job), call `await self?.broadcast(reading)`.

    4. `func stop()`: Cancel streamTask, nil backgroundSession, finish all continuations, clear dict.

    5. `func makeStream() -> AsyncStream<GPSReading>`: Same pattern as StreamBroadcaster.makeStream() from Phase 1 — UUID keyed continuation with onTermination cleanup.

    6. Private `func broadcast(_ reading: GPSReading)` and `func removeContinuation(id: UUID)`.

    Use `.otherNavigation` not `.automotiveNavigation` (avoids road-snapping on mountain terrain — see pitfall 2 in RESEARCH.md).

    Create `protocol GPSManagerProtocol: Actor { func makeStream() -> AsyncStream<GPSReading>; func start() async; func stop() async }` so ActivityClassifier can accept MockGPSManager in tests. Make GPSManager conform.

    Note: GPSReading must be defined in the same file or accessible — define it as a top-level struct (not nested in GPSManager) so MockGPSManager.swift can reference it without importing GPSManager as a type.

    Actually: define `struct GPSReading: Sendable` at file scope in GPSManager.swift (public enough for test target via @testable import).

    Create ArcticEdgeTests/Helpers/MockGPSManager.swift:
    - `actor MockGPSManager: GPSManagerProtocol`
    - `var continuations: [UUID: AsyncStream<GPSReading>.Continuation]`
    - `func start() async` — no-op
    - `func stop() async` — finish all continuations
    - `func makeStream() -> AsyncStream<GPSReading>` — same UUID keyed pattern
    - `func inject(_ reading: GPSReading) async` — broadcasts to all continuations

    Create ArcticEdgeTests/Location/GPSManagerTests.swift:
    - `import Testing; @testable import ArcticEdge`
    - `@Test func testGPSReadingInvalidSpeed()` — GPSReading(speed: -1, horizontalAccuracy: 5, timestamp: Date()) → speed < 0 is invalid
    - `@Test func testGPSReadingInvalidAccuracy()` — GPSReading(speed: 5, horizontalAccuracy: -1, timestamp: Date()) → accuracy < 0 is invalid
    - `@Test func testMockGPSManagerInjectsReading()` — create MockGPSManager, subscribe to makeStream(), inject a reading, confirm it arrives

    Build settings already have SWIFT_STRICT_CONCURRENCY = complete. Add nonisolated annotations wherever needed (e.g., GPSManagerProtocol members must be nonisolated to avoid MainActor bleed — follow WorkoutSessionProtocol precedent from Phase 1).
  </action>
  <verify>
    <automated>xcodebuild test -scheme ArcticEdge -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -only-testing:ArcticEdgeTests/GPSManagerTests -parallel-testing-enabled NO 2>&1 | grep -E "Test Suite|passed|failed|error:"</automated>
  </verify>
  <done>GPSManager.swift and MockGPSManager.swift exist and compile cleanly. GPSManagerTests (3 tests) pass. No SWIFT_STRICT_CONCURRENCY warnings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ActivityManager actor and injectable activity protocol</name>
  <files>ArcticEdge/Activity/ActivityManager.swift, ArcticEdgeTests/Activity/ActivityManagerTests.swift, ArcticEdgeTests/Helpers/MockActivityManager.swift</files>
  <behavior>
    - ActivityManager.start() guards on CMMotionActivityManager.isActivityAvailable() — no-op on simulator, does not crash
    - ActivityManager.stop() finishes all continuations cleanly
    - MockActivityManager.inject(activity:) delivers a CMMotionActivity to all streams
    - ActivityManagerTests verifies stop() finishes streams without hanging
  </behavior>
  <action>
    Create ArcticEdge/Activity/ directory. Create ActivityManager.swift with:

    1. `actor ActivityManager` with:
       - `private let manager = CMMotionActivityManager()`
       - `private var continuations: [UUID: AsyncStream<CMMotionActivity>.Continuation]`

    2. `func start()`: Guard `CMMotionActivityManager.isActivityAvailable()` — if unavailable (simulator), return immediately without error. Call `manager.startActivityUpdates(to: OperationQueue()) { [weak self] activity in guard let activity else { return }; Task { await self?.broadcast(activity) } }`.

    3. `func stop()`: Call `manager.stopActivityUpdates()`, finish all continuations, clear dict.

    4. `func makeStream() -> AsyncStream<CMMotionActivity>`: UUID keyed with onTermination cleanup — same pattern as GPSManager/StreamBroadcaster.

    5. Private `func broadcast(_ activity: CMMotionActivity)` and `func removeContinuation(id: UUID)`.

    6. Define `protocol ActivityManagerProtocol: Actor { func makeStream() -> AsyncStream<CMMotionActivity>; func start() async; func stop() async }`. Make ActivityManager conform. Mark protocol members nonisolated.

    Create ArcticEdgeTests/Helpers/MockActivityManager.swift:
    - `actor MockActivityManager: ActivityManagerProtocol`
    - `func start() async` — no-op
    - `func stop() async` — finish all continuations
    - `func makeStream() -> AsyncStream<CMMotionActivity>` — UUID keyed
    - `func inject(_ activity: CMMotionActivity) async` — broadcasts to all continuations

    Note on CMMotionActivity in tests: CMMotionActivity cannot be initialized directly (no public init). For tests that need a specific activity state, use `MockActivityManager.inject()` with a dummy CMMotionActivity obtained via `CMMotionActivity()` (bare init is available, all booleans default to false). Test the protocol bridge behavior, not the CMMotionActivity values.

    Create ArcticEdgeTests/Activity/ActivityManagerTests.swift:
    - `@Test func testActivityManagerStartIsNoopOnSimulator()` — call start(), confirm no crash (isActivityAvailable() returns false in simulator)
    - `@Test func testActivityManagerStopFinishesContinuations()` — create MockActivityManager, makeStream(), stop(), confirm stream finishes (for await loop exits)
    - `@Test func testMockActivityManagerInjectsActivity()` — inject CMMotionActivity(), confirm received on stream

    Imports needed: `import CoreMotion` in ActivityManager.swift and test files.
  </action>
  <verify>
    <automated>xcodebuild test -scheme ArcticEdge -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -only-testing:ArcticEdgeTests/ActivityManagerTests -parallel-testing-enabled NO 2>&1 | grep -E "Test Suite|passed|failed|error:"</automated>
  </verify>
  <done>ActivityManager.swift and MockActivityManager.swift exist and compile. ActivityManagerTests (3 tests) pass. isActivityAvailable() guard means no crash in simulator.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 ActivityClassifier test stubs</name>
  <files>ArcticEdgeTests/Activity/ActivityClassifierTests.swift</files>
  <action>
    Create ArcticEdgeTests/Activity/ActivityClassifierTests.swift with 11 stub @Test functions — all must compile but all must fail (use `#expect(Bool(false), "stub — not yet implemented")`).

    The stubs mirror the VALIDATION.md per-task map exactly. Use the struct name `ActivityClassifierTests` and wrap in `@Suite("ActivityClassifier")`:

    ```
    // DETC-01: Signal fusion
    testSkiingClassification
    testChairliftRequiresAllThreeSignals
    testTwoOfThreeInsufficientForChairlift
    testGPSBlackoutSustainsChairlift

    // DETC-02: Hysteresis
    testShortSignalDoesNotTransition
    testFullHysteresisWindowTriggersTransition
    testBriefStopDoesNotEndRun

    // DETC-03: RunRecord lifecycle
    testConfirmedSkiingCreatesRunRecord
    testTransitionFinalizesRunRecord
    testEndDayFinalizesOpenRun
    ```

    Each test body: `#expect(Bool(false), "stub — implement in plan 02-02")`

    This file intentionally references no ActivityClassifier type yet — that is created in plan 02-02. The file must compile with just the Testing import and the expect calls. If ActivityClassifier does not exist yet, do NOT reference it in stubs — keep stubs as pure #expect(Bool(false)) placeholders.

    The purpose: verifies the test target can find and run these tests before implementation begins, satisfying the Nyquist rule that every verify has an automated command.
  </action>
  <verify>
    <automated>xcodebuild test -scheme ArcticEdge -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -only-testing:ArcticEdgeTests/ActivityClassifierTests -parallel-testing-enabled NO 2>&1 | grep -E "Test Suite|passed|failed|error:"</automated>
  </verify>
  <done>ActivityClassifierTests.swift compiles. All 11 tests run and fail with "stub" message (not error — expected RED state). Test suite report shows 11 tests, 0 passed, 11 failed.</done>
</task>

</tasks>

<verification>
After all three tasks:

1. `xcodebuild build -scheme ArcticEdge -destination 'platform=iOS Simulator,name=iPhone 16 Pro' 2>&1 | grep -E "error:|BUILD"` — must show BUILD SUCCEEDED with zero errors.
2. `xcodebuild test -scheme ArcticEdge -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -only-testing:ArcticEdgeTests/GPSManagerTests -only-testing:ArcticEdgeTests/ActivityManagerTests -parallel-testing-enabled NO 2>&1 | grep -E "Test Suite|passed|failed"` — GPSManagerTests (3) and ActivityManagerTests (3) pass.
3. `xcodebuild test -scheme ArcticEdge -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -only-testing:ArcticEdgeTests/ActivityClassifierTests -parallel-testing-enabled NO 2>&1 | grep -E "Test Suite|passed|failed"` — 11 tests run, all fail (expected RED).
</verification>

<success_criteria>
- GPSManager actor with CLLocationUpdate.liveUpdates(.otherNavigation) integration exists and compiles under SWIFT_STRICT_CONCURRENCY = complete
- GPSManagerProtocol and ActivityManagerProtocol defined for mock injection
- ActivityManager actor with CMMotionActivityManager bridge exists; isActivityAvailable() guard prevents simulator crash
- MockGPSManager and MockActivityManager actors exist in test target
- 11 ActivityClassifierTests stubs are RED (fail, not error)
- 6 new tests (GPSManagerTests + ActivityManagerTests) are GREEN
- Zero new compiler warnings
</success_criteria>

<output>
After completion, create `.planning/phases/02-activity-detection-run-management/02-01-SUMMARY.md`
</output>
