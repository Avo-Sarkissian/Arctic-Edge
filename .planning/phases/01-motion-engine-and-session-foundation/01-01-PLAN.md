---
phase: 01-motion-engine-and-session-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ArcticEdge.xcodeproj/project.pbxproj
  - ArcticEdge/Motion/FilteredFrame.swift
  - ArcticEdge/Motion/BiquadHighPassFilter.swift
  - ArcticEdge/Motion/RingBuffer.swift
  - ArcticEdge/Motion/MotionManager.swift
  - ArcticEdge/Motion/StreamBroadcaster.swift
  - ArcticEdgeTests/Motion/BiquadFilterTests.swift
  - ArcticEdgeTests/Motion/RingBufferTests.swift
  - ArcticEdgeTests/Motion/MotionManagerTests.swift
  - ArcticEdgeTests/Motion/StreamBroadcasterTests.swift
autonomous: true
requirements:
  - MOTN-01
  - MOTN-02
  - MOTN-03
  - MOTN-04
  - MOTN-05

must_haves:
  truths:
    - "CMDeviceMotion is read at 100Hz and emitted as FilteredFrame without any Sendable violations at build time"
    - "High-pass biquad filter passes a 5Hz sinusoid and attenuates a 0.3Hz sinusoid by at least 40dB"
    - "RingBuffer drain() atomically returns all buffered frames with no samples lost between concurrent append and drain calls"
    - "StreamBroadcaster delivers identical frames to two independent AsyncStream consumers without calling CMMotionManager start more than once"
    - "Thermal state change adjusts deviceMotionUpdateInterval to 50Hz (serious) or 25Hz (critical)"
  artifacts:
    - path: "ArcticEdge/Motion/FilteredFrame.swift"
      provides: "Sendable value type with all IMU primitive fields"
      exports: ["FilteredFrame"]
    - path: "ArcticEdge/Motion/BiquadHighPassFilter.swift"
      provides: "vDSP.Biquad HPF wrapper with Audio EQ Cookbook coefficients"
      exports: ["BiquadHighPassFilter"]
    - path: "ArcticEdge/Motion/RingBuffer.swift"
      provides: "Actor with capacity 1000, O(1) append, synchronous drain"
      exports: ["RingBuffer"]
    - path: "ArcticEdge/Motion/MotionManager.swift"
      provides: "Actor owning CMMotionManager, thermal throttling, AsyncStream emission"
      exports: ["MotionManager", "MotionDataSource"]
    - path: "ArcticEdge/Motion/StreamBroadcaster.swift"
      provides: "Actor that fans out FilteredFrame to multiple AsyncStream consumers"
      exports: ["StreamBroadcaster"]
    - path: "ArcticEdgeTests/Motion/BiquadFilterTests.swift"
      provides: "Failing test stubs for MOTN-02"
    - path: "ArcticEdgeTests/Motion/RingBufferTests.swift"
      provides: "Failing test stubs for MOTN-03"
    - path: "ArcticEdgeTests/Motion/MotionManagerTests.swift"
      provides: "Failing test stubs for MOTN-01 and MOTN-05"
    - path: "ArcticEdgeTests/Motion/StreamBroadcasterTests.swift"
      provides: "Failing test stubs for MOTN-04"
  key_links:
    - from: "CoreMotion callback (OperationQueue thread)"
      to: "MotionManager actor"
      via: "Task { await actor.receive(frame) } after extracting FilteredFrame primitives"
      pattern: "Task.*await.*receive"
    - from: "MotionManager actor"
      to: "StreamBroadcaster actor"
      via: "await broadcaster.broadcast(frame) after filtering"
      pattern: "await.*broadcast"
    - from: "StreamBroadcaster.makeStream()"
      to: "consumer AsyncStream"
      via: "continuation.yield(frame) stored per UUID"
      pattern: "continuation.*yield"
    - from: "ProcessInfo.thermalStateDidChangeNotification"
      to: "motionManager.deviceMotionUpdateInterval"
      via: "Task { await self?.adjustSampleRate(for:) }"
      pattern: "adjustSampleRate"
---

<objective>
Build the motion engine layer: build settings hardened for strict concurrency, test stubs created (Wave 0), then all five motion components implemented and passing (Wave 1).

Purpose: This is the bottom of the dependency stack. PersistenceService (plan 01-02) consumes FilteredFrame and RingBuffer. The ActivityClassifier (Phase 2) consumes StreamBroadcaster. Nothing else can be built until this plan is green.

Output: Five source files in ArcticEdge/Motion/, four test files in ArcticEdgeTests/Motion/, and SWIFT_STRICT_CONCURRENCY = complete in project.pbxproj for all build configurations.
</objective>

<execution_context>
@/Users/avosarkissian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/avosarkissian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-motion-engine-and-session-foundation/01-RESEARCH.md
@.planning/phases/01-motion-engine-and-session-foundation/01-VALIDATION.md

<interfaces>
<!-- Contracts defined in this plan. All five types below are created here from scratch. -->
<!-- Downstream plan 01-02 depends on FilteredFrame and RingBuffer from this plan. -->

FilteredFrame (ArcticEdge/Motion/FilteredFrame.swift):
```swift
struct FilteredFrame: Sendable {
    let timestamp: TimeInterval   // CMDeviceMotion.timestamp
    let runID: UUID
    let pitch: Double             // attitude.pitch (radians)
    let roll: Double              // attitude.roll (radians)
    let yaw: Double               // attitude.yaw (radians)
    let userAccelX: Double        // userAcceleration.x (g)
    let userAccelY: Double        // userAcceleration.y (g)
    let userAccelZ: Double        // userAcceleration.z (g)
    let gravityX: Double          // gravity.x
    let gravityY: Double          // gravity.y
    let gravityZ: Double          // gravity.z
    let rotationRateX: Double     // rotationRate.x (rad/s)
    let rotationRateY: Double     // rotationRate.y (rad/s)
    let rotationRateZ: Double     // rotationRate.z (rad/s)
    let filteredAccelZ: Double    // high-pass filtered userAcceleration.z (carve-pressure axis)
}
```

MotionDataSource protocol (for mocking in tests):
```swift
protocol MotionDataSource: Sendable {
    var deviceMotionUpdateInterval: Double { get set }
    func startDeviceMotionUpdates(to queue: OperationQueue, withHandler handler: @escaping CMDeviceMotionHandler)
    func stopDeviceMotionUpdates()
}
```

RingBuffer public interface:
```swift
actor RingBuffer {
    func append(_ frame: FilteredFrame) async
    func drain() -> [FilteredFrame]  // synchronous body, no await inside
    var count: Int { get }
}
```

StreamBroadcaster public interface:
```swift
actor StreamBroadcaster {
    init(motionManager: MotionManager)
    func makeStream() -> AsyncStream<FilteredFrame>
    func start(runID: UUID) async
    func stop() async
    // internal: func broadcast(_ frame: FilteredFrame)
}
```
</interfaces>

<!-- Build setting context: project.pbxproj currently has SWIFT_VERSION = 5.0 for all configs;
     SWIFT_STRICT_CONCURRENCY is absent. Must be added to all four XCBuildConfiguration blocks
     (Debug + Release for main target; Debug + Release for test target). -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden build settings and create failing test stubs (Wave 0)</name>
  <files>
    ArcticEdge.xcodeproj/project.pbxproj
    ArcticEdgeTests/Motion/BiquadFilterTests.swift
    ArcticEdgeTests/Motion/RingBufferTests.swift
    ArcticEdgeTests/Motion/MotionManagerTests.swift
    ArcticEdgeTests/Motion/StreamBroadcasterTests.swift
  </files>
  <action>
Step A: Build settings in project.pbxproj.

Using a script (ruby -e or python3 -c or direct sed), add `SWIFT_STRICT_CONCURRENCY = complete;` and change `SWIFT_VERSION = 5.0` to `SWIFT_VERSION = 6.0` in ALL four XCBuildConfiguration sections (Debug + Release for ArcticEdge target; Debug + Release for ArcticEdgeTests target). Run `xcodebuild build -scheme ArcticEdge -destination 'generic/platform=iOS Simulator'` to confirm the project compiles clean before any new code is added. If it does not compile clean at this point, fix the existing ContentView.swift or ArcticEdgeApp.swift warnings before proceeding.

Step B: Create test stub files. Create the directory ArcticEdgeTests/Motion/ first. Each file must:
- Use `import Testing` (no XCTest)
- Have a `@Suite` struct
- Contain `@Test` functions that call `#expect(Bool(false), "not yet implemented")` so they fail immediately and clearly
- Use no em-dashes in comments

BiquadFilterTests.swift stubs (covers MOTN-02):
- `testHighFrequencyPasses`: synthesize 100 samples of a 5Hz sine at 100Hz sample rate, apply filter, assert output RMS > 0.9 * input RMS
- `testLowFrequencyRejects`: synthesize 100 samples of a 0.3Hz sine at 100Hz sample rate, apply filter after 50-sample warmup, assert output RMS < 0.05 * input RMS (40dB attenuation)
- `testInitDoesNotCrash`: init BiquadHighPassFilter(cutoffHz: 1.0, sampleRate: 100.0) without crashing

RingBufferTests.swift stubs (covers MOTN-03):
- `testDrainReturnsAllAppended`: append 5 frames, drain, assert count == 5 and all frames present
- `testDrainEmptiesBuffer`: append 3 frames, drain, assert buffer count == 0
- `testCapacityDropsOldest`: append 1001 frames to a capacity-1000 buffer, assert count == 1000 and first frame was dropped
- `testConcurrentAppendAndDrain`: run 10 concurrent tasks appending frames while draining; assert total frames never exceed capacity and no crash

MotionManagerTests.swift stubs (covers MOTN-01, MOTN-05):
- `testStartEmitsFrames`: mock MotionDataSource delivers 3 motion callbacks; assert MotionManager emits 3 FilteredFrame values on its stream
- `testThermalNominalIs100Hz`: call adjustSampleRate(for: .nominal), assert interval == 0.01
- `testThermalSeriousIs50Hz`: call adjustSampleRate(for: .serious), assert interval == 0.02
- `testThermalCriticalIs25Hz`: call adjustSampleRate(for: .critical), assert interval == 0.04

StreamBroadcasterTests.swift stubs (covers MOTN-04):
- `testTwoConsumersReceiveSameFrames`: make two streams, broadcast 3 frames, assert both consumers receive all 3 frames in order
- `testSingleMotionManagerStart`: assert that calling makeStream() twice does not trigger a second MotionManager start
- `testConsumerCancellationCleansUp`: cancel one consumer's stream, assert the other consumer still receives frames
  </action>
  <verify>
    <automated>xcodebuild test -scheme ArcticEdge -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -only-testing:ArcticEdgeTests/Motion 2>&1 | tail -20</automated>
  </verify>
  <done>
- project.pbxproj contains SWIFT_STRICT_CONCURRENCY = complete in all four build configurations
- SWIFT_VERSION = 6.0 in all four build configurations
- All four test stub files exist under ArcticEdgeTests/Motion/
- Test suite compiles and tests run but fail with "not yet implemented" (red, not compile error)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement FilteredFrame, BiquadHighPassFilter, and RingBuffer</name>
  <files>
    ArcticEdge/Motion/FilteredFrame.swift
    ArcticEdge/Motion/BiquadHighPassFilter.swift
    ArcticEdge/Motion/RingBuffer.swift
  </files>
  <behavior>
    - FilteredFrame: Sendable by composition of Sendable stored properties (all let, all value types). No conformance declaration needed if compiler infers it; add explicit `Sendable` if needed for SWIFT_STRICT_CONCURRENCY = complete.
    - BiquadHighPassFilter: NOT Sendable (stateful). `final class`. Init computes Audio EQ Cookbook second-order HPF coefficients from cutoffHz and sampleRate. Default cutoffHz = 1.0, Q = 0.707. Coefficient array order for vDSP.Biquad: [b0/a0, b1/a0, b2/a0, a1/a0, a2/a0]. apply(_ sample: Double) -> Double wraps input in [sample], returns [0].
    - RingBuffer: `actor`. Capacity constant = 1000. append(_:) drops oldest when full. drain() body is fully synchronous (let chunk = buffer; buffer = []; return chunk) with NO internal await. count: Int computed property.
    - BiquadFilterTests pass: 5Hz input preserved, 0.3Hz input attenuated.
    - RingBufferTests pass: drain atomicity, capacity overflow, concurrent stress.
  </behavior>
  <action>
Create ArcticEdge/Motion/ directory. Implement the three files.

FilteredFrame.swift: Define the struct with all 15 fields listed in the interfaces block above. Add `filteredAccelZ: Double` as the 15th field (high-pass filtered carve-pressure value, computed by MotionManager before constructing the frame). No init customization needed; use memberwise.

BiquadHighPassFilter.swift: Implement exactly as shown in RESEARCH.md Pattern 6. Expose a single public constant `kFilterCutoffHz: Double = 1.0` at the top of the file (module-level, not inside the class) for future calibration. Comment block above init must explain: "cutoffHz is a starting hypothesis; calibrate against real ski data before treating as settled."

RingBuffer.swift: Implement exactly as shown in RESEARCH.md Pattern 2. The drain() method comment must explain the no-await constraint: "Synchronous body prevents actor reentrancy window where new frames could be lost between read and clear."

After implementing each file, go back to the test stub for that file and replace the `#expect(Bool(false), ...)` stubs with real assertions calling the actual implementation. The tests must be RED before implementation and GREEN after.

No em-dashes anywhere in comments or doc strings. Use colons or sentence structure instead.
  </action>
  <verify>
    <automated>xcodebuild test -scheme ArcticEdge -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -only-testing:ArcticEdgeTests/Motion/BiquadFilterTests -only-testing:ArcticEdgeTests/Motion/RingBufferTests 2>&1 | grep -E "Test run|passed|failed|error"</automated>
  </verify>
  <done>
- ArcticEdge/Motion/FilteredFrame.swift exists with all 15 fields, compiles under SWIFT_STRICT_CONCURRENCY = complete
- ArcticEdge/Motion/BiquadHighPassFilter.swift exists, vDSP.Biquad initialized, apply() works
- ArcticEdge/Motion/RingBuffer.swift exists, drain() has no await in body
- BiquadFilterTests: testHighFrequencyPasses and testLowFrequencyRejects green
- RingBufferTests: all four tests green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement MotionManager and StreamBroadcaster actors</name>
  <files>
    ArcticEdge/Motion/MotionManager.swift
    ArcticEdge/Motion/StreamBroadcaster.swift
  </files>
  <behavior>
    - MotionManager: actor. Owns BiquadHighPassFilter (not Sendable; safe because it is actor-isolated). Defines MotionDataSource protocol for test injection. startUpdates(runID:) sets deviceMotionUpdateInterval = 0.01 (100Hz), calls motionDataSource.startDeviceMotionUpdates. Callback extracts all primitives immediately, applies filter to userAccelZ, constructs FilteredFrame, bridges via Task { await self.receive(frame) }. receive(_:) appends to RingBuffer and calls await broadcaster.broadcast(frame). Thermal observer registered in startUpdates; adjustSampleRate(for:) sets interval on the live data source.
    - StreamBroadcaster: actor. Stores [UUID: AsyncStream<FilteredFrame>.Continuation]. makeStream() uses AsyncStream.makeStream() tuple API. broadcast(_:) calls continuation.yield(frame) for all stored continuations. onTermination removes the continuation via Task { await self.removeContinuation(id:) }.
    - MotionManagerTests pass: mock MotionDataSource emits 3 callbacks, MotionManager emits 3 frames; thermal interval tests pass.
    - StreamBroadcasterTests pass: two consumers receive identical frames; single start assertion passes.
  </behavior>
  <action>
MotionManager.swift:

Define `MotionDataSource` protocol at the top of this file:
```swift
protocol MotionDataSource: AnyObject {
    var deviceMotionUpdateInterval: Double { get set }
    func startDeviceMotionUpdates(to queue: OperationQueue, withHandler handler: @escaping CMDeviceMotionHandler)
    func stopDeviceMotionUpdates()
}
extension CMMotionManager: MotionDataSource {}
```

The actor stores: `private let dataSource: any MotionDataSource`, `private let ringBuffer: RingBuffer`, `private let broadcaster: StreamBroadcaster`, `private let filter: BiquadHighPassFilter`, `private var currentRunID: UUID = UUID()`.

startUpdates(runID: UUID): set currentRunID, set dataSource.deviceMotionUpdateInterval = 0.01, capture `let capturedRunID = runID` before calling startDeviceMotionUpdates (per RESEARCH.md Open Question 4 -- do not read actor-isolated currentRunID from callback).

stopUpdates(): call dataSource.stopDeviceMotionUpdates(), finish broadcaster streams.

observeThermalState(): register NotificationCenter observer. Handler extracts thermalState then calls `Task { await self?.adjustSampleRate(for: state) }`. Register inside startUpdates.

adjustSampleRate(for:): set dataSource.deviceMotionUpdateInterval to 1/100, 1/50, or 1/25 per thermal level. This is an actor-isolated func so it is safe to mutate dataSource here.

StreamBroadcaster.swift: Implement exactly as shown in RESEARCH.md Pattern 3. Add a `start(motionManager: MotionManager)` method that calls `await motionManager.startUpdates(runID:)` passing a fresh UUID. The actor must hold a reference to the MotionManager to call startUpdates once.

In MotionManagerTests.swift: create a `MockMotionDataSource` class (not actor) that conforms to MotionDataSource. It stores the handler closure and exposes `func deliverMockMotion(_ motion: CMDeviceMotion)` for test control. Replace all stubs with real assertions.

In StreamBroadcasterTests.swift: inject a mock MotionManager or call broadcaster.broadcast(_:) directly to verify fan-out behavior. Replace all stubs with real assertions.

No em-dashes in any comments.
  </action>
  <verify>
    <automated>xcodebuild test -scheme ArcticEdge -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -only-testing:ArcticEdgeTests/Motion 2>&1 | grep -E "Test run|passed|failed|error"</automated>
  </verify>
  <done>
- ArcticEdge/Motion/MotionManager.swift and StreamBroadcaster.swift exist
- Project builds with zero warnings and zero errors under SWIFT_STRICT_CONCURRENCY = complete
- All MotionManagerTests and StreamBroadcasterTests are green
- Full Motion test suite: xcodebuild reports all tests passed
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, run the full Motion test suite:

```
xcodebuild test -scheme ArcticEdge -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -only-testing:ArcticEdgeTests/Motion 2>&1 | tail -30
```

Expected outcome: All tests pass. Zero build errors. Zero Sendable warnings (they are errors under SWIFT_STRICT_CONCURRENCY = complete).

Secondary check: build for simulator without running tests to confirm zero-warning compile:

```
xcodebuild build -scheme ArcticEdge -destination 'platform=iOS Simulator,name=iPhone 16 Pro' 2>&1 | grep -E "warning:|error:|BUILD"
```
</verification>

<success_criteria>
1. project.pbxproj has SWIFT_STRICT_CONCURRENCY = complete and SWIFT_VERSION = 6.0 in all four build configurations
2. Five source files exist under ArcticEdge/Motion/ with correct actor and type declarations
3. Four test files exist under ArcticEdgeTests/Motion/ with real assertions (no "not yet implemented" stubs remaining)
4. BiquadFilterTests: 5Hz sinusoid passes (RMS ratio > 0.9), 0.3Hz sinusoid rejected (RMS ratio < 0.05)
5. RingBufferTests: drain atomicity, capacity overflow, and concurrent stress tests all pass
6. MotionManagerTests: frame emission and all three thermal interval assertions pass
7. StreamBroadcasterTests: two-consumer fan-out and single-start assertions pass
8. Project compiles with zero warnings and zero errors under strict concurrency
</success_criteria>

<output>
After completion, create `.planning/phases/01-motion-engine-and-session-foundation/01-01-SUMMARY.md` following the summary template at `@/Users/avosarkissian/.claude/get-shit-done/templates/summary.md`.
</output>
